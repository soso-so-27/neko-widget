{
  "name": "neko-official-window-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "wrangler dev --local"
  },
  "devDependencies": { "wrangler": "4.125.0" }
}
