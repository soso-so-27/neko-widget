# Build22本人結果と次の限定比較

2026-09-10。本人共有 `DA45705C-8F2E-4522-8B78-B876D153FEEB/1-テキスト.txt`。
Build22が本人端末で実行された証拠を受領。App Store Connectへ再ログインして同じ配布を確認する依頼は不要。

## 確定した結果

- selected21 / previouslyConfirmed21 / remaining0 / reviewActions0。判断は全件復元。未操作・確認忘れではない。
- Nano追加は候補13枚で実行。failed0 / oneRegion11 / noCatRegion2 / multipleRegions0 / withheld0。
- 候補一致12/13=92.3%、A3/4、B9/9。両方を単独候補にする誤り1/5は残存。
- 正しい単独候補の不要保留も0だが、目的の改善も0。合成での救済2件は本人写真への改善ではなかった。
- 距離保留3枚の保留前順位は本人と一致2・反対猫1。単なる距離閾値緩和は採らない。
- 誤候補1枚が追加検出0か1のどちらだったかは集計から不明。小ささ・遮蔽・姿勢・NMSなどを原因と断定しない。
- 本アプリの変更なし、写真/ID/特徴量共有なし。accuracy/product/goalValidated=falseは正しい。

既存の独立レビュー担当も上記集計と不確実性を確認。本人判断は学習・閾値調整へ還流しない。

## 次の比較を結果を見る前に固定

公開の合成fixtureだけで、公式YOLOX-S (640)を1候補だけ比較する。Nano (416)のモデルと入力解像度の両方が変わるため、良くなっても単独の原因証明とはしない。
出典: https://github.com/Megvii-BaseDetection/YOLOX/tree/main/demo/ONNXRuntime
モデル: https://github.com/Megvii-BaseDetection/YOLOX/releases/download/0.1.1rc0/yolox_s.onnx
35,858,002 bytes / 初回取得SHA256 `c5c2d13e59ae883e6af3b45daea64af4833a4951c92d116ec270d9ddbe998063`。
公式assetにdigestなし。HTTPS公式assetから取得した同一バイトを今後固定する値で、upstream署名の証明ではない。

既存3生成画像＋75%四隅12＋ペア6の計21、同一PC描画・NMS0.45・score0.3・CPU2threads。
閾値・配置を追加探索しない。出力は別名・新規限定。個人写真は一切入力しない。
期待条件：Nanoが残した縦ペア2件の救済、既に検出できた4ペアを維持、単独15の複数誤検出0。
範囲重ね合わせも目視する。失敗すればそのままiOSへ配布せず、方式の限界を記録する。
PC結果はiOS/本人の結果ではない。通過してもiOS実装での比較・保存21枚の再計算が必要。
検出精度の採用目標と時短目標は変更しない。

## PC初回結果と追加で分かった問題

`s640-fixed-fixtures.json`: 15単独は全て1領域、6ペアは4件2領域・2件3領域、計4.770秒。
全6ペアの重ね合わせを目視。縦配置grayの顔と体へ重複2枠が付いている。
その問題を隠さず、相手猫だけを取り除きgrayの縮尺・位置を維持した反証画像を1件追加。
`s640-single-counterfactual.json`: 1匹なのに2領域。従ってS640へ置換して領域数だけで保留する方式は**不採用**。
この反証ケースは追加後の新しい合格標本として精度へ算入しない。閾値探索も行わない。

## 最小の保守的な修正候補

次に比較するのは「元写真内の利用可能な検出枠の中に、互いに重ならない2枠が存在するときだけ保留」。
顔/体の重なった枠だけなら保留しない。枠を統合せず、範囲数を猫の頭数と断定しない。
本物の2匹が重なった写真を救えない可能性は残る。見逃し解消/全複数猫対応とは表示しない。
切り出し・個体特徴・本人判断は不変。保存済み21枚を再利用。
配布前は固定21枚+反証1枚で、ペアの追加救済を維持、単独/反証の不要保留0、iOS実装で同じ条件を確認する。
本人の誤候補が「検出0なのか1なのか重なりなのか」を次は区別できるよう、共有は状態×本人選択の集計のみ追加する。

独立レビューの指摘を取り込み、境界を固定：写真内の可視枠に**正の隙間**がある組だけを対象にし、辺が接触するだけなら未解決。
同じ猫の離れた部位を別々に囲めば誤保留が残り得る。本物の2匹が重なる場合は救済できない。
1つのgray反例だけへの対応を避け、iOS比較は元21枚＋全6ペアそれぞれ各側を単独で残す12対照、計33構成を事前固定した。
同じ画像・配置が重複する対照は独立標本ではない。テストが合格しても製品精度と呼ばない。
iOSゲート：旧見落とし4ペアを追加保留、6ペア全部に離れた枠、27単独/対照の不要保留0。
SモデルはNanoに重ねず置換し、同梱は従来通り個体識別モデル＋一般物体検出モデルの2つだけ。
モデルファイルは約3.7MB→35.9MB。端末時間/メモリはPC・Simulator数値で保証しない。
共有v9は状態6種×本人判断6種の件数のみ追加し、保存形式・本人判断・個体推論・距離規則は不変。
