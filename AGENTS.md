# Codex 作業方針

このリポジトリでは、正しさと安全性を保ちながら、モデルのトークン、ツール呼び出し、CI待機、ユーザーの確認回数を必要最小限にする。

## 基本

- 最初に変更範囲、受け入れ条件、必要な検証を一度だけ定める。
- `rg` と対象箇所の狭い読み取りから始め、関係のない全ファイル、全ログ、全ADRを読まない。
- ログはエラー周辺と必要な集計だけを読み、同じ資料を複数担当で重複解析しない。
- サブエージェントは、独立した並行作業が速度または品質を明確に改善するときだけ使う。同じ差分を同じ観点で再監査させない。
- 進捗共有は開始、重要な発見、検証結果、完了に絞り、変化のない待機報告をしない。
- 取得できないトークン数や費用を推測で実測値のように示さない。

## 変更規模

- **小変更**: 原則メイン担当だけで実装し、対象テストと静的確認だけを行う。現在情報や外部仕様が必要な場合を除き、サブエージェントや広範な調査を使わない。
- **中規模変更**: 必要なら独立レビューを1人だけ使う。対象テストを先に通し、候補SHAのCIを1回確認する。
- **高リスク変更**: 永続データ、写真・個体所属、プライバシー、共有、署名、Widget、リリースは実装と独立レビューを分ける。サブエージェントは原則2人以内で役割を重複させず、cheap checks → 対象テスト → 候補SHA CIの順で検証する。必要なprivacy、署名、migration、fail-closed確認は省略しない。

## CI・配布

- 1つのCI runは1人だけが監視し、意味のある変化かterminal結果だけ共有する。
- 最初の具体的なエラーへ絞って修正し、同一SHAでgreenの検証を理由なく繰り返さない。
- push、main更新、TestFlight配布はユーザーの依頼範囲に含まれる場合だけ行う。配布時は候補SHA CI → main CI → TestFlightの順で確認する。
- 軽微な修正ごとに配布せず、関連修正を一つのrelease candidateへまとめる。

## コンテキストと完了

- 確定事項は短い作業メモとして一度だけ保持し、過去の長いログや報告を毎回展開しない。
- `NekoWidget/docs`、`NekoWidget/実装_Codex指示書.md`、外部資料は現在の変更に直接関係するものだけ読む。
- 機能やrelease batchが変わるときは新しいCodexタスクを提案できるが、ユーザーの依頼なしに作成しない。
- 要求された挙動とリスク相応の検証が通った時点で完了し、「念のため」だけの追加監査、ビルド、配布を続けない。
- 最終報告は結果、重要な検証、残る既知事項だけを簡潔に伝える。
