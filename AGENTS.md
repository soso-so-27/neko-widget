# ねこのまどの開発

写真を主役に、Widget・思い出・まどを直感的に使える体験を整える。猫登録や共有は任意。猫個体識別の研究は本線の条件にせず、未コミット変更がある研究worktreeを編集しない。

## 作業と完了

- 依頼の範囲・完了条件・必要な検証を最初に一度決め、実装、結果の確認、必要な修正まで続ける。既に許可された可逆な編集やローカル検証のたびに承認を求めない。
- `rg` と関係箇所の読み取りから始める。スキルは実際の用途が合うものだけ使い、必要な参照先だけ読む。全資料・全ログ・全スキルを毎回展開しない。
- 未確認事項は実装済みと区別する。実機でしか得られない結果の待機中も、独立した本線作業は進める。通常利用の同じ写真・場面で確かめられる項目はまとめる。
- 要求された結果と必要な検証が揃えば完了。「念のため」だけの再監査や同一SHAの成功済み検証を追加しない。新たな変更・失敗・具体的懸念が出た範囲だけ追加確認する。

## リスクに応じた検証

- 文言・配置・指示文など、動作やデータを変えない変更は差分と該当表示・参照先を確認する。表示変更には実画面または製品の描画結果を使い、コード読解だけで体験確認済みとしない。
- 動作変更は影響経路の既存テストを優先する。小変更は主担当で扱い、実装をなぞるだけのテストや無関係な一式検証を増やさない。
- データ消失、写真の所属・送信先の混同、権限・暗号化・共有解除、署名、Widgetの保存・更新・リンク先の整合性に関わる変更は、独立レビューと必要な境界検証を行う。privacy・署名・migration・fail-closedの条件を省略しない。
- 「共有」「Widget」というファイル名だけでリスクを決めない。この分類は実装・レビューの判断基準であり、CIの必須ジョブや証拠判定を手動で省略する許可ではない。

## 必要なときに読む資料

- 次のタスクや現在の実装状態を整理するとき: [現在の台帳](handoffs/2026-09-13-current-task-board.md)。そこから今回に関係する記録だけ読む。
- 候補push・CI・main反映・TestFlightを扱うとき: [開発・配布手順](handoffs/development-release-workflow.md)。既存の成功証拠の再利用条件と配布CLIを使う。
- 公式まどの補充・障害・問い合わせ対応: [運用窓口](handoffs/2026-09-13-operating-desk.md)。予定配信の詳細が必要なら、その参照先へ進む。
- 設計・APIの変更: `NekoWidget/docs`、実装指示書、公式仕様のうち今回の判断に必要な部分だけ読む。

## 権限と並行作業

- push・main反映・配布は既存の依頼範囲を引き継ぐ。一般公開・外部招待や審査提出・課金開始へ範囲を広げない。追加承認が必要な操作は、準備を済ませた具体的な候補を示して確認する。
- Appleへのアップロード成功を通常の証拠にし、毎回の画面確認・再ログインを開発条件にしない。処理完了・実機確認とは区別する。
- サブエージェントは独立した作業で速度か品質が改善するときだけ使う。同じ差分の同じ観点を重複監査しない。利用者のセッション保存制約を守り、成果を主担当へまとめる。
- CI監視は1人で担当する。未計測の時間短縮を実績とせず、報告は結果・検証・残る既知事項を簡潔に示す。
