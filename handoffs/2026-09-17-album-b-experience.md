# アルバム：横送りのピックアップと固定の入口

利用者が比較後に選んだB案を実装するバッチ。起点は origin/main `7388fce`（アプリ177）。独立worktree `C:/dev/neko-album-b-experience-20260916`、branch `codex/album-b-experience-20260916`。個体識別研究には触れていない。

## 変更

- ピックアップは表紙1枚、最大3件の横送り。次のカードが見える幅にし、自動送りは行わない。表紙は固定枠を満たし、既存の猫位置を考慮した切り抜きを使う。詳細は原本全体を表示する。
- テーマはアイコンと短い名前。月の写真・ムービー・昔と最近・年から探すは同じ形式の固定入口に揃える。年の一覧をトップへ展開しない。
- お気に入りはアルバムに維持し、上部のブックマークから開く。保存データや共有範囲は変更しない。
- 猫名は全体を曖昧に絞り込むスイッチから、猫別アルバムへの入口へ変更。猫IDを写真・比較・ピックアップの遷移に引き継ぐ。全体の月・動画を個別猫の作品として表示しない。
- 月の写真は既存の写真ブラウザーへ統一。時系列の先頭を表紙にし、左右送り・拡大・お気に入り・接続済みまどへの送信を再利用する。
- ピックアップの候補IDと閲覧履歴を端末内へ保存。同日中は順序を維持し、翌日は未閲覧・最近見ていない候補を優先する。最大3件、同一写真・近い撮影場面の重複を避ける。非同期で届いた候補は次に画面を開いたとき空き枠へ補い、閲覧中に並べ替えない。Widgetの更新周期は変更しない。

## 検証と完了条件

変更経路の既存UIテストを更新し、猫別→全体への復帰、月の写真の保存状態、横送りの復帰、大きい文字、0件/権限なしを確認する。写真の所属・権限境界と推薦履歴を別担当がレビュー。Swiftの選定・日付・永続化検証を追加し、既存CIへ含める。CIのscopeや必須ジョブは変更しない。workflow内の旧ナビゲーション文字列照合だけ、猫IDを持つ現行の経路へ更新する。

ローカル安価検証、候補CI、製品描画、mainの成功証拠再利用、内部TestFlightの順に進める。公開・審査提出・課金開始は対象外。完了証拠は後続の配布記録に残す。

標準/最大文字の製品描画で見た目を確認する。実写真全件の切り抜きや、複数日使った際の楽しさ・継続率が最適と証明されたわけではない。人や複数猫の関係が切り抜きで失われる写真は、具体的な事例に基づき代表選択・描画の改善対象とする。

設計根拠：`C:/dev/neko-evidence/album-b-experience-design-20260916.md`。新しい独立ホームやタブ増設は行わず、写真を開く目的と探す目的を区別する。

## 候補CIで発見した修正（9月17日）

`4a9e2e1` の候補CI `35174784985` はビルド・runtime・smoke・Widget描画成功、アプリUI 45件中4件失敗。失敗を無視して配布せず、添付AX階層と製品PNGを確認した。

- 月の保存2件は操作後に「お気に入りに追加済み」が実在。ただし親Groupの識別子が保存・同日の写真など子の識別子を上書きしていた。月ブラウザーを独立コンテナーにし、子の操作を別々に認識できるよう修正。
- 猫の入口も、親AnyLayoutの識別子が2つの猫ボタンを上書きしていた。子を保持するアクセシビリティコンテナーへ修正。
- 最大文字の月一覧では、写真ブラウザーの上に一覧が残る実際の遷移不具合を確認。pathによる遷移と直接Viewを渡す遷移の混在を解消し、今回の月・動画・年・猫の一覧も型付き経路に揃える。待機時間を増やして回避しない。

標準文字・最大文字のピックアップ描画では、表紙が枠を満たし、次カードの一部が見えることを確認。テーマは標準2列・最大文字1列。描画はシミュレーターのfixture写真であり、実写真全件の切り抜き評価とは区別する。修正後のCIと配布証拠は別途追記する。

## 完了証拠：Build 178

- 製品SHA `9620e8219107ac7fe936ded5ce3927e7edb1f01c`。候補pushの[CI 35180840771](https://github.com/soso-so-27/neko-widget/actions/runs/35180840771)で必要8項目成功。アプリUIは45件、失敗0。上記4件の失敗経路もすべて通過した。
- 同SHAをmainへ反映し、[main CI 35184059564](https://github.com/soso-so-27/neko-widget/actions/runs/35184059564)が候補の成功証拠を再利用して成功。必須確認の手動省略は行っていない。
- 配布CLIのdry-runで前回177・今回178・SHA・CI・既存内部配布設定を照合して1回dispatch。対象runの既存testflight環境だけを承認した。
- [TestFlight 35184150897](https://github.com/soso-so-27/neko-widget/actions/runs/35184150897)成功。9月17日14:07:14 JSTの `UPLOAD SUCCEEDED with no errors` を確認。Apple処理完了、内部グループ表示、178の利用者実機操作は未確認。公開・外部招待・審査提出・課金開始は行っていない。
- 最終候補のPNG6枚（標準/最大文字ピックアップ、猫別から全体へ戻った固定入口、月の2枚目を保存して再度開いた状態、最大文字の年一覧・お気に入り）を実際に開いて確認。証拠は作業checkoutの `output/app-ui-35180840771/`、対応表は `manifest.json`。全録画を含む大容量ZIPは取得せず、必要なPNGのみHTTP Rangeで取得した。
- 安価検証：開発フロー8群46.4秒、既存境界63件（成功、環境条件による1skip）、差分チェック成功。最終CIのジョブ開始〜完了は49分37秒、アプリ操作自体は41分4.7秒。今回をCI全体の高速化の実績とはしない。

実機で残る評価は実写真の表紙の切り抜きと、横送り・固定入口の分かりやすさ。長期の新鮮さ・継続効果を検証済みとはしない。月/年/猫の遷移と保存状態をユーザーへ繰り返し試験依頼する必要はなく、具体的な再発があればその経路だけ再開する。
