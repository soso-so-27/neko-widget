# N19 猫の見どころとテーマの入口

利用者が「意味のある小特集＋固定テーマ」を承認。origin/main `20b0f41` から専用worktree・branch `codex/album-discovery-20260916` で実装。**Build 176のAppleアップロードまで完了**（2026-09-16 15:10:08 JST）。

## 完了条件

- 見どころの直下で猫のテーマを選べる。年・時期の一覧でテーマを埋めない。
- サムネは見どころ2枚、テーマ1枚ずつ、年・過去一覧・お気に入り0枚。標準はテーマ2列、大きな文字は1列。見どころ内部は3〜6枚を既存の写真ビューアで直接めくる。
- 見どころは現在対象の猫写真に限定。近接撮影30分以内をまとめ、同じテーマ・年月で3場面以上ある場合だけ作る。これは類似画像認識ではなく時間による重複抑制。
- 候補は全件を一覧に残し、同じ特集を再び開ける。初期の選定周期は週単位。同じ週・同じ素材では同じ結果。毎日新しい内容を約束しない。
- 新着の月・季節は同じコンパクトな上部枠に表示。既読処理と保存先を維持する。写真が少ない・権限がない場合に空の特集へ誘導しない。
- Widgetの表示・回数・直接遷移、共有・公式写真・課金・個体識別は変更しない。

## 構成と判断

上部の大きなコラージュをコンパクトな2枚へ。テーマは識別に役立つ代表1枚を維持し、可能なら隣接カードの同一写真を避ける。人物・複数猫・おでかけでは全体を表示し、猫だけの拡大で文脈を失わないようにする。

AppleのCollections/Layoutは視覚的な一覧と端末・文字サイズへの適応を推奨するが、サムネの普遍的な最適枚数は示していない。2枚/1枚は本画面の情報量と猫の見やすさに対する設計判断であり、一般法則や継続率の実証ではない。

- https://developer.apple.com/design/human-interface-guidelines/collections
- https://developer.apple.com/design/human-interface-guidelines/layout
- https://www.nngroup.com/articles/mobile-list-thumbnail/
- 設計の調査と比較: C:/dev/neko-evidence/album-widget-structure-review-20260916.md

## 検証状況

- Windows上の開発手順チェック8件成功（76.3秒）、共有・Widget境界の既存検査63件（1件既存skip）成功。
- 独立レビューで見つかった、隣接表紙の重複fallbackと詳細から戻った後の基準日更新を修正。
- 選定の意味ある境界を既存Swift検証に追加。製品写真ビューアへつなぐ標準/大文字、再表示、少数写真・権限なしのUI検証を追加。
- 候補CI `35050007116` は旧ルート識別子の固定照合が残り失敗。workflowの照合文字列を新しい分岐へ合わせた（検証範囲・必須条件の変更なし）。
- 次の候補CI `35050408682` はbuild・runtime・smoke・Widget描画3種が成功。アプリUIは45件中44件成功し、新しい見どころの写真送り・再表示・少数・権限なしも成功。
- 最大文字で季節の行から戻り上へスクロールする既存テストが停止。録画と操作ログから、最初の逆スクロール直後に応答が止まることを確認。固定セクションの内側LazyVStackをVStackにし、高さ計算の入れ子を解消。fixture側の同一画像ロード再登録による不要な変更通知も抑制した。両者の寄与の比率は未確定であり、環境障害として扱わない。同じ操作を維持して再確認する。
- CIの製品描画で標準の見どころ2枚＋テーマ1枚、最大文字の折り返しを確認した。描画はテスト用イラスト。実写真の選定の魅力と長期継続効果は別の確認事項。
- CI `35054516268` でも同じ最大文字・逆スクロールの停止が再現した。内側だけの修正では不十分だった。外側LazyVStackはアルバム棚全体を一つの巨大な子として扱う構造が残っていたため、有限個の棚を並べるrootもVStackへ変更。写真グリッドと過去一覧の遅延描画は維持する。テスト操作の省略や成功までの同一SHA再試行はしない。
- 最終候補 `4dae16ac6bb21fd73a453640f4ac60c646357cbe` の[CI 35058482059](https://github.com/soso-so-27/neko-widget/actions/runs/35058482059)で必要8ジョブ成功。アプリUIは45件すべて成功し、問題の最大文字・逆スクロールも同じ操作のまま107.0秒で成功した。標準の見どころ＋テーマ、最大文字のテーマ復帰後の製品描画を再確認。外側の巨大なlazy子をやめた変更で再現が解消したが、全OSの内部原因まで特定したとはしない。
- 同一SHAをmainへ反映し、[main CI 35061710013](https://github.com/soso-so-27/neko-widget/actions/runs/35061710013)で候補の成功証拠を再利用。release CLIのdry-run後、同じSHA/build指定で内部TestFlightをdispatchした。
- [TestFlight 176 / run 35061828793](https://github.com/soso-so-27/neko-widget/actions/runs/35061828793)の署名・entitlement・export・uploadが成功。Appleアップロード完了は15:10:08 JST。Apple処理完了、内部グループ画面、176の利用者実機確認は未照合であり、通常の開発・配布の前提にしない。一般公開・外部招待・審査提出・課金開始は行っていない。

## 所要時間と残る確認

- 最終候補CIは46分09秒、mainの成功証拠再利用は15秒、配布jobは8分36秒。初回の古い検査文字列と、その後のスクロール不具合で追加CIが必要になった。今回の全作業が短縮できたとは報告しない。
- CIの状態待ちはスクリプト内にまとめ、job完了・失敗でのみモデルへ結果を返した。画像取得も既存のHTTP Rangeで必要画像のみ抽出し、約600MBのartifact全体取得を避けた。モデル・履歴・リセット権は変更していない。
- 表示証拠: `C:/dev/neko-evidence/album-discovery-20260916/final/`。合成イラストによるレイアウト・操作検証であり、実際の猫写真の組み合わせが魅力的か、再訪や課金につながるかは未実証。新しい必須検証の待ち条件にはしない。
- 上部の選定は同じ週・同じ候補集合なら安定。新しい対象写真や権限・猫の範囲が変われば候補も変わる。標準のWidget日替わりと「もう一枚」は変更していない。

研究worktreeは編集していない。履歴・モデル設定・リセット権・自動化は変更していない。
