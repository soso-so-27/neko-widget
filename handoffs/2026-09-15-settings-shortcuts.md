# 設定への中継画面を減らす

2026-09-15。利用者が前回の4候補を実装として承認。最新 `origin/main 96279fe` を確認し、`codex/settings-shortcuts-20260915` / `C:/dev/neko-settings-shortcuts-20260915` で作業。猫個体識別の研究worktreeは変更しない。

## 変更

1. 設定→猫のプロフィール→選んだ猫のプロフィールへ直行。写真タブ→猫の写真という閲覧目的の入口は維持。
2. まどの設定で、名前の横の鉛筆から標準の入力ダイアログを開く。「接続と端末」へ入り直さず変更できる。相手・追加端末の編集制限、既存の名前検証と暗号化共有は維持。
3. 写真設定に「写真の対象」と「除外した写真」の入口を分ける。対象選択は既存のアルバム選択画面を再利用。限定アクセス・対象失効時の案内と変更後のスキャンを維持する。
4. 未完了のまどが1件だけなら、人＋からその設定へ直接進む。0件の場合は既存の作成・参加・以前のまどへの追加から選ぶ。読み込み失敗、切替中、まど数の上限を解除しない。

完了条件：既存の選択/編集/キャンセル/戻り先が動き、標準・大文字の対象画面を確認する。保存先、相手、Photos権限、削除確認を変更しない。必要CIが成功した同一候補をmainへ反映し、内部TestFlight1回へまとめる。

## 保存の境界

- 名前ダイアログは開いた共有spaceIDを保持。既存 `PairingViewModel.updateWindowDisplayName` に任意の期待spaceIDを渡し、保存直前のfresh operation stateと照合する。別まどや共有終了後へ書き込まない。旧PairingViewの呼び出しは従来どおり。
- 名前編集の準備は画面の `.task(id:)` に属し、離脱時の取消・request一致を確認する。遅れて入力ダイアログを開かない。保存失敗時に過去の成功文言を流用しない。
- 猫のプロフィール保存・写真所属・削除、対象アルバム保存/再スキャンの実処理は変更していない。
- UI fixtureは実保存や通信を行わない。見た目と操作の確認を、実際の名前共有成功の証拠と混同しない。

## 確認と配布

- 独立レビュー：写真/プロフィールの入口と名前保存先・編集権限・非同期終了を確認。名前編集の遅延表示と古い成功文の誤表示を候補push前に修正し、指摘解消を確認。
- 既存UIテスト3件を新しい導線へ更新。独立にselectorとfixtureの整合を確認。新たな全画面テストやtoolbarの固定寸法判定を追加していない。
- ローカル：共有/Widget境界62成功＋既存skip1件、pairing-only設定7件成功、開発経路8群成功（32.5秒）。`git diff --check` 成功。
- CI・製品画像・main反映・TestFlight：作業中。成功後に結果を追記する。

## 別件の検討

「各端末で気に入った写真が一か所に集まる」という利用者の実感は、[共有まどの設計検討](2026-09-15-shared-favorites-concept.md)へ整理。今回は検討のみで、送受信写真の統合、永続共有アルバム、写真の自動送信を本バッチには実装していない。
