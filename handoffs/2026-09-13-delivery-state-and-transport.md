# 送信状態の反映と通信診断

起点main `b7f1fac`。作業先 `C:/dev/neko-delivery-ops-20260913`、branch `codex/delivery-ops-20260913`。研究worktree、CI構成、共有サービスの仕様は変更しない。

## 今回分かったこと

- 利用者が提供したBuild144の診断では、handoffを処理した同期の完了から、`sent=1`を記録した同期の完了まで76.134秒ある。この差は写真uploadだけの所要時間ではなく、再試行待ちと同期の別処理を含み得る。当時の失敗段階・通信理由は復元できない。
- 現行コードでは、foreground側の同期で送信台帳だけが変わっても、共有画面へ早期通知されない。画面自身の同期が走っていなければ進捗の500ms更新もなく、ハート取得・まど名同期が終わるまで古い表示が残り得る。この経路はコードで確認した不具合。過去の76秒すべての原因とは断定しない。
- `performRequest`はHTTPエラーを保持する一方、URLSessionの通信エラーを一律`retryableServer`にしていた。145以降の段階別診断でもタイムアウト・切断・オフラインの区別は失われていた。

## 変更と完了条件

写真の送受信が安全な境界まで完了した時点で送信台帳の変化も画面に反映し、独立したハート・名前同期の待ちを上乗せしない。受信ストリームの途中や失敗した状態を先行公開せず、全体の同期成功通知と相手への到着は別のままにする。

通信失敗はタイムアウト／オフライン／途中切断／接続先へ接続できない／安全な接続を確立できない／取消／不明の固定値だけを保持する。予約・upload・commitの開始時刻を既存の短い相関値とともに記録し、どの段階で待ったか追えるようにする。NSErrorのuserInfo、URL、本文、写真ID、認証情報は保持・出力しない。

接続設定、鍵、送信写真、同じ送信要求ID、日次上限、再試行間隔は維持する。サーバー受付を相手到着・閲覧として表示しない。元の「ねことも」の接続失敗原因、過去の76秒の内訳、実回線の短縮効果は未確定。

## 検証方針

既存の送信状態／同期の回帰確認、Swift core verifierの固定通信分類・再試行不変、診断情報の非公開境界を確認する。共有に関わる変更のため独立レビュー後、候補SHAの必要CIを一度実行する。Windowsの静的検証とMac上のSwift／製品実行、実機の通信結果は区別して記録する。

## 並行した準備

[指定1名への外部テスト資料](2026-09-13-external-one-test-preparation.md)を公開3まどと現行運用に合わせた。外部審査・招待・掲載は実行していない。公式配信は既存の9/27朝までの予定を継続し、新たな配備や写真制作は行っていない。補充と問い合わせは[運用窓口](2026-09-13-operating-desk.md)を使う。

検証・配布の最終結果は下へ追記する。

## 候補固定前

実装と独立レビューを相互に分担。送信状態の通知はreceiveChanges正常復帰後、inbox/outbox/outgoingOutcomesの差分を確認し、MainActor内でもlifecycle tokenを再検証する。ハートの通知は別に維持。対象の既存境界3件成功、診断privacy13件中12成功・既存Swift実行1skip、開発flow事前確認成功（32.1秒）、diff確認成功。Swift coreには13種類の入力で分類、ユーザー文言、初回再試行間隔不変を追加。WindowsではSwiftは未実行。
