# CI短縮の次候補

状態：候補コードと手元検証まで。Macでの実行・時間短縮は未確認。本線へは未適用。

作業場所は `C:/dev/neko-ci-balanced-ui-20260913`、ブランチは `codex/ci-balanced-ui-20260913`。公開まど共通化 `6d22fe7` を基点とする。前の失敗候補 `ae2790e` は採用していない。

## 変更

- 通常のfixtureを `build-for-testing` で1回ビルドする。
- 生成した `.xctestrun` を直接読み、AとBの専用Simulatorへ `test-without-building` を並行実行する。Xcodeの自動分担は使わない。
- AはSoloとOfficial（現行24件）、BはComposerとCatProfile（10件）。件数より過去の所要時間で分ける。クラス単位で指定するため、クラス内の新しいテストも含まれる。
- 両方の終了を待ち、終了コード、個別ログ、xcresult、画像を保存する。どちらかの失敗を成功として扱わない。
- UI用Simulatorの終了後、Galleryの通常・白背景＋長文＋大文字・文字なしを、それぞれ新規Simulatorで順次確認する。通常は同じビルドを再利用し、残る2条件だけ設定別にビルドする。
- 既存のsmoke、iOS 18.5／26.2のruntime、Widgetの全サイズ・画像pixel判定を維持する。追加のMacジョブは作らない。

同じOfficial UIの2回実行は、設定だけでなくOSも異なる（smokeは18.6、sharingは26.2）ため維持した。前案のクラス指定と単一メソッドskipの混在はやめ、`-only-testing` のみで重複しない組を作る。

ビルド済みのテスト製品を `.xctestrun` で実行する方法は、[Appleのコマンドラインテスト資料](https://developer.apple.com/library/archive/technotes/tn2339/_index.html)でも示されている。実際のXcode 26.3上での実行成功は、この資料とは別に確認する。

## 検証と採用条件

手元では分担5件、既存契約12件、準備helper5件、Bash構文2本を確認。対象クラスの和集合・重複なし・将来のメソッド追加・未知クラスの拒否を含む。独立レビューで、共有build DBを避ける `.xctestrun` 直接指定と、曖昧なonly/skipの併用を修正した。

過去の失敗した自動分担では長い側が約26分だった。今回のクラス単位分担は約20分となる参考試算だが、起動時間・同じMac上の並行負荷を含む全体短縮を示すものではない。最初の約17分ずつの案は採用しない。

次はこの候補のCIを1回実行し、全対象成功、Gallery3条件成功、総時間・分担時間・新規Simulatorの起動時間を比較する。失敗や短縮効果なしなら本線へ採用しない。公開まど共通化や159の利用を、この実験の完了待ちにしない。

Mac計測の結果と候補SHAは後から追記する。現時点で「CIがさらに速くなった」とは報告しない。
