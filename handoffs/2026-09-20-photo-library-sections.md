# 写真タブにお気に入りとメモをまとめる

## 採用した整理

利用者の「アルバムのお気に入りアイコンもなくなるってこと？」への説明と、その後の「それですすめて」に対応。ホームタブは追加しない。

- アルバム：自動でまとめた写真、ピックアップ、月・ムービーを見る。右上のお気に入りと独立した「写真と言葉」の入口を外す。
- 写真：同じ画面の「すべて／お気に入り／メモあり」で切り替える。初期は「すべて」。個別写真のお気に入り操作とメモの編集は残す。
- メモあり：写真とメモの抜粋を一覧にし、開くと全文を読む。既存メモと明示的に保管したメモを継続して読める。
- すべて：現在の猫写真と、iCloudに明示保管した写真コピーを表示。元写真が閲覧可能で保管時の明示リンク・版が一致する場合だけ重複を避ける。
- 設定：iCloudの保管と復元を管理する。写真一覧を見るだけで保管や送信を始めない。

## データ境界

永続化形式、CloudKit、共同記録、送信、課金は変更しない。端末メモと保管コピーは異なる内容を上書きしない。保管した写真はPhotoKit権限と独立して閲覧できるが、アカウントを固定して読み、背景化・アカウント変更時はコピーの表示を消す。元写真は現在の対象範囲とPhotos権限を維持する。

まどの送信履歴と共同記録の統合、課金価値・容量・解約時の約束はこのバッチの完了対象ではない。今回の整理で販売可能になったとは扱わない。

## 確認

- 独立レビュー：遷移登録位置、元写真の範囲、コピーのアカウント境界、読み取りが保存再試行を起こさない点を確認。重大な指摘なし。
- 製品SHA `b2ee2bfc005197aa984154ae86424751563dac0b`、候補CI [35481146106](https://github.com/soso-so-27/neko-widget/actions/runs/35481146106) はBuild・実行時・Photos権限/実スキャン・関連9操作が全成功。22分00秒、runner合計56.63分。
- main CI [35482155503](https://github.com/soso-so-27/neko-widget/actions/runs/35482155503) は同一SHAの候補成功を再利用し21秒で成功。
- **TestFlight 1.0 (192)** は [release35482274760](https://github.com/soso-so-27/neko-widget/actions/runs/35482274760) により **2026年9月20日10:56:38 JSTにAppleアップロード成功**。ログは `UPLOAD SUCCEEDED with no errors`。対象SHA/build一致を確認して既承認のtestflight環境だけ承認した。
- 写真一覧の3区分、お気に入り、検索から戻った画面、大きい文字のメモ詳細を対象の実描画で確認。これはfixtureによる対象画面の確認であり、利用者の全データ/全画面の確認とは区別する。
- Appleの処理完了・192実機確認・別の実iPhoneへの復元は未確認。毎回のApple画面操作は行っていない。
- 実機確認済みとは扱わない。利用者にはお気に入りと既存メモへの入口を一度確認してもらう。

最終候補のcommit（10:20:32 JST）からアップロードまで36分07秒。それ以前のCI条件の見落とし、検索UIの修正失敗、テストの期待値不一致も含む今回全体は大幅な時間超過であり、20〜30分の目標達成とはしない。失敗の経過は以下へ保持する。公開・課金・外部テスト招待は行っていない。

証拠の保存先：`C:/dev/neko-evidence/photo-library-sections-20260920`。最終描画は `product-passed-ui/ios-26-2/composer-screenshots/`。利用者に求める確認は「写真→お気に入り」「写真→メモあり」で今までの内容が開けること。記録の作り直しやアプリの削除は求めない。

## 候補で見つかった検証側の不備

最初の製品候補a485ecfのBuildは、workflowに直書きされた旧albums-favoritesの存在チェックで停止した。移設に伴うPython検証とUI操作は追従したが、.github側の旧期待を見落とした。製品に不要な旧IDを戻す回避はせず、実際のお気に入り一覧を示すsaved-memories-galleryへ期待を更新した。

workflowの変更を含むため、49d2038は既存のfail-closed規則によるfull検証になった。最初の限定候補の成功とは扱わず、手戻り時間も記録する。

78c81a3でも同じstepの古いaccessibilityLabel期待が停止原因になった。現在の一覧は同じ枚数をaccessibilityValueで提供しているため期待を修正。workflowの当該静的shellブロック全体を抽出してGit Bashで実行し、後続の全grep/awk/件数条件までexit 0を確認した（`C:/dev/neko-evidence/photo-library-sections-20260920/photo-boundary-static-current.log`）。先頭の一件だけで再pushした確認不足を記録する。

49d2038のrun35471758947は、Build・実行時・写真権限/スキャン・Widget描画など他7jobが成功したが、app-uiが75分上限に到達。全体78分56秒、runner合計165.33分。53操作開始・49成功・3失敗・1未完了であり、成功とは扱わない。失敗には変更後の空状態文言と設定の戻り操作へのテスト追従不足が含まれる。検索失敗時の回収画像では検索入口だけでなく「おふろ」の絞り込みも成功しており、検索機能が欠落したとは判断しない。製品の標準検索UIは維持し、失敗した操作位置を修正する。

検索については詳細から一覧へ戻った後、ナビゲーションバーと検索欄が両方消える状態を失敗時AXと録画最終フレームで確認した。これは製品側の検索・画面遷移の問題として扱う。標準searchableのisPresentedを明示管理し、詳細を開く時と一覧を離れる時に検索の表示状態を終了させる。検索文字列は保持する。テストは別区分への切り替えで回避せず、戻った画面の検索欄とバーの復帰、検索解除後の元メモ表示まで確認する。API根拠は[Appleの検索表示管理](https://developer.apple.com/documentation/swiftui/managing-search-interface-activation)。

210a5edのrun35477112325は20分50秒、runner合計51.02分。Build・権限/実スキャン・runtimeは成功、関連9操作は8成功/1失敗。上部バーは復帰したが検索入口が消えたままの状態を実画像/AXで確認し、main反映・配布を停止。次候補では詳細を開く直前の検索状態を保持し、一覧へ戻るときに同じ検索状態を再開する。検索中にタイトルバーを隠す標準UIは維持し、検索欄の再到達→解除→通常のバーと元メモ復帰を確認する。

48d9251のrun35478373708も同じ1操作が失敗。他8操作とBuild/権限/runtimeは成功、全体30分52秒・62.65 runner分。失敗操作自体は30秒で終了しており、30分間検索で固まった証拠ではない。復帰状態だけの修正では解消しないため、最終候補は一覧内に常設する標準UISearchBarへ変更する。NavigationStackの検索用バーの表示状態とは独立させ、検索語・キャンセル・戻った後の入口を一覧自身で保つ。テストは同じ実際の利用経路を継続し、区分切り替えや再起動で回避しない。

a305af7のrun35479947156は23分45秒・49.03 runner分、8/9成功。戻った後のバー/検索欄/検索語保持と再入力は通過し、復帰画面も確認した。失敗はネイティブ取消ボタンを「キャンセル」と仮定したテスト側の指定で、実AXは「閉じる」。アプリ本体の変更は追加せず、観測済みのボタン名へテストを修正する。

全画面75分を再実行するために製品を変えることはしない。別のCI-only候補46c4f22で、旧/新双方を許容する上記2箇所の静的検査への正確な移行だけを既存CI選択scopeとして検証する。製品や他の検査の変更は引き続きfullとなる。成功後に同じworkflowを製品へ取り込み、既存reviewed-memory-read-ui-v2の固定9操作と必須Build/境界検証へ戻す。今回の所要時間は20〜30分の目標を超過しており、速度改善達成とは報告しない。
