# 写真のメモ：共通の作成・編集と明示保管

2026-09-20。基点 `78149b9`。状態：製品 `2693498`、Build 193のAppleアップロード成功。端末表示・Apple側処理完了とは区別する。

## 利用者に見える変更

- 写真・本人のWidget・アルバムの共通写真ブラウザの鉛筆を上部へ。作成・編集の呼び方は「メモ」に統一。
- メモあり一覧を維持。端末メモと保管記録の詳細に共通の大きな写真・本文・補足日付を使い、同じ位置の鉛筆から同じ編集画面を開く。
- 小さなサムネイル＋「写真を見る」の中間表示を廃止。大きな写真を押して拡大・通常写真操作へ進む。お気に入り・まどへの送信は維持。
- 設定の「写真と言葉を選ぶ」と専用composerを削除。設定は保管状況・読込・未完了の再試行に限定。
- 通常写真の「…」にメモの保管を追加。最初に、選んだ写真とメモ、および以後の本文変更をiCloudへ反映することを明示する。
- 完了済みの保管ラベルは本文の下に常設しない。未完了・競合は説明と確認先を出す。

## 保存と移行の境界

`PhotoMemoCoordinator` が端末メモと明示的に選んだ保管記録をつなぐ。端末のNoteStore schema3に同意したaccount・record ID・固定operation ID付きの反映待ちを原子的に保存する。CloudKit schemaと暗号化/署名構成は変更しない。

- メモ作成だけではuploadしない。古いコピーも閲覧だけでは自動反映を有効にしない。
- 旧明示リンクと一致した本文を編集する場合は、反映開始の確認を行ってから一つの編集対象にする。関連が不明・本文が異なる記録は勝手に統合しない。
- 端末へ保存してから保管先へ反映。中断・応答喪失・連続編集で同じ内容を増殖させず、後続の編集も残す。保管先への反映が完了するまでは「保管済み」にしない。
- 共通IDは既存の保管record IDを使用。別端末の未知PhotoKit IDを推測で結び直さない。
- 取得済みの変更は、明示同意済みで端末の反映待ちがない場合だけ取り込む。同じ写真・本文の二重表示は明示関係と版で抑える。
- 本文削除は写真を保持する。元から本文だけの記録に限り空になった記録を削除。保管コピー全体の削除は別操作で、端末の元メモを削除しない。
- 異なる本文は双方を提示し、利用者の選択を版確認付きで反映する。写真そのものが異なる競合・削除競合は上書きせず保護する。
- Apple Account変更を跨いで別アカウントへ送らない。自分用メモをまどの「ひとこと」に自動で入れない。

## 検証範囲

- 保存担当が既存保管verifierへ、明示同意・オフライン再開・同時編集・削除・アカウント・競合選択の境界を追加。主担当が保存層を独立レビュー。
- 画面担当とは別に既存UIテストを新経路へ追従。通常写真からの作成、メモ一覧からの編集/検索復帰、旧記録の復元/編集、保管後の本文更新を再利用。廃止composer専用のPhotosPickerテストとseedを削除。
- development-flow全10群成功（41.1秒）。関連静的境界63件中62成功・既存skip1。diffcheck成功。これをnative成功と取り違えない。
- 保存層変更をread-only UI変更とは扱わない。現CI規則の必要範囲を使い、完了済みの同SHAは再実行しない。

配布後の利用者確認は、既存「はじめてのおふろ」と端末メモが同じ配置・同じ鉛筆から開けることを中心にする。記録の作り直し、アプリ削除による復元テストは依頼しない。実2端末の復元・反映、課金価値の検証は自動fixtureの成功と区別する。

## 根拠

承認設計：[写真に付くメモを一つの体験にする](2026-09-20-simple-photo-memo-experience.md)。
保存層：`PhotoMemoryNoteStore.swift`、`PersonalArchiveStore.swift`、`PhotoMemoCoordinator.swift`。
画面：`PhotoMemoryNoteView.swift`、`PhotoMemoryNoteLibraryView.swift`、`PersonalArchiveView.swift`、`LikedPhotosView.swift`、`SettingsView.swift`。

## 最初の候補の結果

- `1dea5a9` / CI `35485948913`: Build（保存verifier含む）・権限/実scan・runtime・Gallery全系統成功。画面操作53件中51成功、2失敗。全体64分26秒、app-ui約64分。内部upload未実施。
- メモ削除は日本語キーボードへの連続削除の後に文字が残るテスト操作。フォーカスとキーボード表示を待ち、一文字ずつ結果を観測して削除するよう修正。空になったこと・削除確認・元写真維持のassertionは維持。
- ピックアップ再表示は最大文字表示で対象カードが画面上方に隠れているのにisHittableがtrueとなり、戻り先のカードを押せないテスト操作。画面録画でもテーマ一覧に留まっていることを確認。カード中心がnavigation barを越えるまで縦スクロールし、表示を確認して押す。遷移/同じ写真/一覧位置のassertionは維持。
- 製品の詳細画面・共通editor・旧保管記録・反映後本文のnative画像を確認。大きな写真＋本文＋鉛筆の共通構成を確認済み。
- 証拠: `C:/dev/neko-evidence/unified-photo-memo-20260920/`。CI時間の限定監査も同フォルダの `release-timing-notes.md` へ集約。未実装の高速化を今回の短縮実績とは扱わない。

## 最終結果

- 最終製品SHA: `269349890a2288e498dc8d3171c8b8fbffcdb927`。初回候補から製品コードは変えず、テスト操作2件のみ修正した。
- [候補CI 35489148667](https://github.com/soso-so-27/neko-widget/actions/runs/35489148667): full-v1の全必須成功。アプリ画面53件0失敗。メモ削除のライフサイクル75.883秒、ピックアップ再表示252.949秒で成功。保存verifier・権限/実写真scan・runtimeも成功。
- 同候補のgallery-normal初回はSpringBoardが「ねこのまど」を検索結果に出さず失敗。画像・ログ・同製品の前回成功・同SHAの他Gallery成功からテスト端末の索引状態と分類し、対象jobだけ1回再実行して成功。アプリ53操作や他の成功jobは再実行していない。
- [main CI 35493314460](https://github.com/soso-so-27/neko-widget/actions/runs/35493314460): 19秒。同一SHA・成功済み候補の実行証拠を再利用。
- 配布CLIのdry-runで全必須証拠、未使用のBuild 193、既存media-staging構成を照合し、同じ引数で1回dispatch。既存testflight環境の当該runだけ承認。保護ルール変更・Apple画面の再ログイン・外部配布・課金は行っていない。
- [TestFlight 35493368458](https://github.com/soso-so-27/neko-widget/actions/runs/35493368458): **1.0 (193)を2026-09-20 15:15:33 JSTにAppleへアップロード成功**。署名・検証・upload jobも成功。Apple側処理完了や内部グループ画面の表示確認は行っていない。
- 利用者の既存データを作り直す必要はない。確認は「写真→メモあり」で既存の保管メモ/端末メモが同じ配置と鉛筆から開けることにまとめる。実iPhone 2台での同時編集・別端末復元は未確認であり、mock transport/Simulator成功と混同しない。

## 所要時間と次の改善

初回候補確定12:11:14→upload15:15:33: **3時間04分19秒**。テスト操作の最終修正13:26:28→upload: **1時間49分05秒**。20〜30分目標は未達。最後の配布job自体は約9分で、mainの証拠再利用も機能したが、全画面操作1jobの待機・テスト操作修正・Gallery索引不調を含む全体を速度改善済みとは扱わない。

初回の最初の失敗03:30 UTCから53操作終了04:14 UTCまで約44分かかり、修正着手も遅れた。今回の保存変更はread-only専用の既存短縮枠では扱えない。次バッチでは、メモ保存変更の厳密な対象選択、必須の保存/権限/移行/通信境界、変更した入口の代表操作を組み合わせる。fullが必要な場合も別Simulator・別checkoutの固定2群へ分け、両群成功を要求する案を検証する。現製品の合格条件を途中で弱めていない。

一次証拠は `C:/dev/neko-evidence/unified-photo-memo-20260920/` の `candidate-ci.json`、`candidate-ci-2.json`、`candidate-ci-final.json`、`main-ci.json`、`release-dry-run.json`、`release-dispatch.json`、`testflight-upload.json`、`upload-step-evidence.json`。限定CI監査は `release-timing-notes.md` に集約。子担当だけの履歴に依存しない。
