# 個人保管：使用量を本人の画面に表示

## 範囲と完了条件

基点 `a41f46f`、独立worktree `C:/dev/neko-preservation-usage-ui-20260923`。既存の本人専用 `GET /v1/usage` を、既定OFFの「サービスに保管」画面へ接続する。課金資格・商品容量・保存/削除/共有/CloudKit・実サービス配備は変えない。TestFlightは出さない。

- 初回一覧は容量APIを待たず表示。別途取得した暗号化済みコピー・メモの使用量/設定上限/空き、保存準備中の予約があればその容量を表示。件数の内部上限を「残り写真枚数」に変換しない。
- 容量結果の版・会計方式・非負/整合を検査し、不正/取得失敗を空き容量0や無制限として見せない。本人session・画面世代・ownerの切替後に古い結果を見せない。期限切れも既存閲覧と使用量確認を課金の有効性に依存させない。
- fixtureの正しい応答、矛盾する応答、会計不能、通信中の本人切替をruntimeで確認し、既存の会員期限切れUI経路で表示を確認する。必要CIはpreflightの実判定に従い、同じ候補で一度だけ実行する。ネイティブ表示変更なのでコード読解だけで完成としない。

この画面に表示される `limitBytes` はサービス構成の現値であり、[50GiB案](2026-09-23-preservation-offer-options.md)を正式販売条件として採用するものではない。実サービスは既定OFF・未配備。

既存の`reviewed-managed-preservation-app-v2`は固定済みの別差分であり、本件には流用しない。変更前に参照した`full-v1`候補CI実測は64.43分（失敗）、97.92分（再試行後成功）。既定30分目標を超えると判断し、今回だけ必要な全件確認を一候補・配布なしで実施する計画に組み直す。開始後に失敗すれば最初の具体的な原因を切り分け、無関係なjobや同SHA成功の繰返しは行わない。これは今回の作業が97.92分で完了するという約束ではない。

## 結果

候補 `83c6445` を `codex/preservation-usage-ui-20260923` にpushした。通常CI [35794251013](https://github.com/soso-so-27/neko-widget/actions/runs/35794251013) はplan・Build・Simulator smoke・runtime・Gallery 3条件の7 jobが成功。一方app-uiは75分上限でcancelled。XCTest 62件中3件が失敗しており、成功扱いにしない。watcher実測はCI全体76.6分、Mac runner 186.5分。初回候補からの時間を後続診断でリセットしない。

- 保管画面の既存テストは、最初の会員リンク失敗メッセージを一覧末尾へ置いていたため `PhotoPermissionUITests.swift:1480` で止まった。`d1c691c` でフィードバックを上部へ移動。3件だけの診断 [35800772455](https://github.com/soso-so-27/neko-widget/actions/runs/35800772455) は、エラー表示・再リンク・容量表示まで到達。iPhone 17 Proのfixture画像でも「使用中 1 MB / 10 GB」「空き容量 10 GB」と、原本を含まない注記を確認した。保管記録が下へ移ったのにUIテストがスクロール前に行を検索し、同テストは記録タップで失敗。`1efcce7` で記録へスクロールしてから押すよう変更した。
- `1efcce7` の保管画面1件診断 [35802355080](https://github.com/soso-so-27/neko-widget/actions/runs/35802355080) は、さらに手前の2回目の「会員情報を接続」ボタンの `enabled && hittable` を5秒以内に満たさず失敗。録画ではボタン自体は画面に存在する。非同期のbusy/画面位置/AX判定のどれかは未確定であり、変更後の記録タップと詳細・持ち出しまで成功したとは記録しない。
- 別の2件は写真一覧の戻り位置と、同じ日の写真でお気に入り保存要求 `true` の後にボタン表示が「お気に入りに追加」のままという失敗。3件診断では前者が成功、後者が再現。後者の `LikedPhotosView.swift` と `AppStoreScreenshotFixture.swift` は今回の差分に含まれず、古い写真状態を保持した詳細画面の表示更新が疑わしい。失敗を無関係として無視せず、別の修正範囲で原因と確認方法を決める。

この候補は**main未反映・TestFlight未配布**。実サービスのOFF/未配備も維持。これ以上、75分の全件を根拠なく再実行しない。次は保管UI診断の状態判定を安定化し、同じ日の写真のお気に入り表示を確定した保存状態へ連動させる設計を別バッチで検討する。その後、現行mainとの差分とCI計画を再評価してから本線反映する。初回候補から診断2回まで約2時間を要し、既定30分目標は未達。コスト問題を「次回メモ」だけで済ませず、app-ui全62件を1jobに詰め75分上限に達する検査方法を見直す。
