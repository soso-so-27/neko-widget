# 写真保管・まどのアルバム：進行の正本

## 目的

猫の写真を安心して残せて、まどで送り合った写真もあとから見返せる状態へ進める。利用者に担当間の資料転送や作業の選び直しを求めない。この会話の主担当が統合と結果確認を受け持つ。

## 今回の二つの作業

| 作業 | 今回の到達点 | 今回は行わないこと |
| --- | --- | --- |
| 自分の写真の保管 | 設定・選択写真からの明示保管・一覧詳細・既存ZIP書き出しをアプリへ接続。既定OFF | 実サービス有効化、自動移行、課金、基盤契約 |
| このまどのアルバム | 既存の共同記録を写真一覧から選んで読み返す。写真を取り下げても二人のメモへの入口を残す | 自動保管、保存期間・上限・共有人数・暗号・権限の変更 |

まどのアルバムは「ここへ追加した写真」が対象。通常の送受信履歴を自動で永久保存したことにはならない。追加者と撮影者・元送信者を混同しない。将来の共有保管案は `2026-09-22-window-album-proposal.md`（別設計候補）のままであり、今回の画面改善によって採用されたことにはしない。

## 担当と現在地

- 本線担当は `2026-09-22-next-session-handoff.md` で途中差分を引き継ぎ済み。以降はこの主担当が引き受け、本線へ独立CI起動を依頼しない。
- 個人保管の最後の `needsResume` 修正とtarget/export接続を独立再レビュー済み。限定範囲でブロッカーなし。Apple認証復帰の実証や実build成功とは区別する。
- まどの画面は別worktreeで実装・独立レビューし、この候補へ集約する。
- CI選択は既存の完全before/after固定方式を使う。workflow、署名、privacy、Photos、両OS runtime、成功再利用条件は変更しない。選択器の差分は製品と分けてレビュー・commitする。未知差分はfullへ戻す。

## 完了条件と費用管理

1. 二つの製品差分が一つの候補に入り、保存先・公開範囲の既存条件を変えていない。
2. 本体/拡張build、Photos bootstrap、両OS runtime、必要な画面操作を一度の候補CIで確認する。
3. 画面操作はOFF時の保管入口非表示と、共同記録の写真選択・本人編集・相手メモ保持・取り下げ・失効。描画画像も確認する。
4. 失敗は製品・テスト・環境に分け、原因に絞って直す。担当別の二重CIや同一成功の再実行をしない。

近い4-job構成の実測は15分03秒〜20分09秒。今回の初回scopeは未計測であり、この時間内の完了は保証しない。初回候補からの失敗・経過時間も結果に含める。今回はTestFlight配布なし。

## 実利用を始める前に残るもの

実Apple認証、鍵管理・会員本人確認・JPEGデコードのprovider、容量と保持/削除/復元運用、別端末確認。既定OFFの接続完了は、バックアップサービスの稼働やデータ保全の保証ではない。これらは今回のUI変更から独立させて進める。

## 結果

候補確定・検証後に追記する。現在は未配布。
