# 個人Widgetの自動再発見と「もう一枚」

2026-09-14。ユーザー依頼は検討結果の1〜4（補充、日次操作、履歴、選定・体験確認）を進めること。
起点は最新 `origin/main` ac19716／内部TestFlight 166。別worktree `C:/dev/neko-personal-rediscovery-20260914`、branch `codex/personal-rediscovery-20260914`。

## 状態

1〜3の実装と、4の技術・製品描画確認は完了し、本線反映済み。TestFlight 167は2026-09-14 15:17:29 JSTにAppleへのアップロード成功。本人のiPhoneでの反映時間・再発見の楽しさは未確認であり、合成fixtureでの動作検証とは分ける。

## 実装範囲

- 個人写真だけにversion付きApp Groupの候補pool、時刻基準の巡回、発行履歴を追加。共有・公式まどの配信には適用しない。
- 最初の候補から表示を始め、最大10写真・約5秒の作業単位で段階補充。PhotoKitの同期要求自体が時間を使う場合は、その1件の終了後に時間予算を判定する。目標100候補・JPEG400ファイル・Widget描画最大2件を別々に制限する。
- 前景の補充と、個人写真専用BGAppRefreshTask。後者は接続・共有同意・通知許可を条件にしない。OSが実行しなければ有限poolを再利用する。
- 写真の対象正本を非同期準備より先に更新。除外・対象変更中は一時停止し、候補の最終公開にrevisionを要求する。Providerの発行参照確保とキャッシュ掃除も同じプロセス間排他を使う。
- 保存による旧20枚の再抽選と、準備しただけのlastShownAt/shownCount加算をやめる。実閲覧が計測できたとは説明しない。
- このiPhone全体で1日1回、準備済みの別写真へ切り替える。写真と使用記録のatomic保存が確定点。失敗・別候補なしは未消費。日付をまたぐ古いentryは当日分を勝手に消費しない。
- 写真タップは166の個別写真入口を使用。保存は右下、「もう一枚」は左下。使用後はチェックへ変え、履歴リンクへ意味を切り替えない。
- 結果と押した直前の写真を48時間参照。既存写真詳細メニュー、Widget案内・設定から履歴へ進む。原本がiCloudにある場合も履歴のキャッシュを利用。保存・送信は既存の個別写真画面へ渡す。

## 今回固定しない仮値

- 6:3:1の配分は今回の補充改善と同時に導入しない。既存の候補選定の重みを利用し、実写真の選定結果を見て調整する。
- 100候補は上限目標。保護中画像と容量次第で少なくなる。20分は標準の予定間隔であり、iOSの実表示時刻を保証しない。
- 日次制限が継続利用や課金価値を証明したとは扱わない。まず埋もれた猫写真に出会う品質を確認する。

## 検証の対象

- 0/1/2/20/21/100候補、同一枠の再要求、サイズ別の画像欠損、補充・退役・除外後の巡回。
- 日付・時差・同時押し・保存失敗・確定後再実行・履歴48時間・改訂の競合。
- UIは製品の履歴/写真/Widget部品を使うfixtureで、通常文字と大きな文字、使用前後、写真を直接開く、保存/送信入口/閉じるを確認する。
- 実機確認はまとめて、①もう一枚で別写真になる、②保存しても戻らない、③履歴から同じ結果/直前へ戻れる、④選ばれた写真が似たものばかりでない、を通常利用の中で確認する。長期監視や全画面の再試験を前提にしない。

## 候補検証の記録

- c59943fのmacOS実行でStore境界・8プロセス同時操作を通過。Small/Medium/Largeの使用前後6枚を製品Widgetの描画で確認し、写真面積・操作の重なり・使用後の配置に問題なし。Galleryの画像は個別ボタンの実タップ検証とは区別する。
- 履歴の通常文字・候補1件・既存写真への移動・保存/送信入口は通過。大文字fixtureの画像からsheetへの文字設定伝播漏れを発見し、明示伝播と縦/横配置の実寸assertを追加した。14a797dの実画像で文字拡大・全幅の縦並び・写真詳細の操作に欠けや重なりがないことを確認した。
- 初回スキャンの集計は旧キャッシュ履歴の存在を前提として失敗。新poolの正本・参照・実JPEG・サイズ・容量を検証するよう置換し、Vision/画像読み込み/描画/更新順序の条件は維持した。新キャッシュ作成経路にも既存の完了イベントと実測描画値を記録する。
- focused artifact検証15件と既存権限bootstrap9件が通過。旧候補の不足ログを補造したり、旧キャッシュ履歴を余分に生成して成功扱いにはしていない。

## 本線反映と配布

- 製品SHA: `14a797d69e309808bd5fc93091a62ddb108c60e9`。
- [候補CI 34808797099](https://github.com/soso-so-27/neko-widget/actions/runs/34808797099): 必須8項目すべて成功。54分52秒（Mac開始待ちを含む）。今回の範囲に必要な検証を実行し、CI高速化の試作は追加していない。
- iOS 18.6のsmoke UI 21件、iOS 26.2のapp UI 42件が失敗0。日次操作と48時間履歴の2件は両方で通過。Widget3サイズ×使用前後6枚の製品描画も通過。既存の写真からの送信・ひとこと編集の操作も成功した。
- スキャンしたfixture 3枚は猫2枚/非猫1枚となり、新poolの候補2件・3サイズ6 JPEG（914,692 B）・正しいrendererを確認。100候補の境界は別のnative Store検証で確認しており、2件のsmokeを100件の実機実績とは扱わない。
- [main CI 34812212799](https://github.com/soso-so-27/neko-widget/actions/runs/34812212799): 同一SHAの成功証拠を再利用して17秒で成功。
- [内部TestFlight 167 / run 34812332977](https://github.com/soso-so-27/neko-widget/actions/runs/34812332977): 配布CLIのdry-runでSHA・必須CI・番号重複・内部設定を確認し、同じ引数で1回dispatch。対象を照合したtestflight環境を承認。archive/export/署名確認が成功し、15:17:29 JSTに `UPLOAD SUCCEEDED with no errors` を確認。run全体も成功。Appleの処理完了や内部グループ表示は追加照合していない。

## 更新後の一度の確認

2026-09-14追記：167で「Widgetが空白、アプリを開いても戻らない」と利用者から報告、その後167のまま表示復帰。旧候補の移行時に空stateを先行させる不具合を修正し、[168へ反映・アップロード](2026-09-14-personal-widget-blank-recovery.md)した。新しい候補の準備が失敗しても利用できる旧画像を引き継ぎ、候補不足時は次の取得対象へ進む。実機事象の直接原因は未確定で、168の実機確認と区別する。

アプリを一度開いてから「自分の写真」のWidgetで、もう一枚→保存→写真詳細のメニュー「まどでめくった写真」から結果/直前を開く。別写真への切り替え、保存で戻らないこと、同じ写真を見返せることを続けて確認できる。選定の好みは普段の写真で判断し、問題が具体化した箇所だけ調整する。

Appleの処理完了・内部グループ表示、実機での更新タイミング、実写真の選定品質・楽しさは未確認。一般公開、外部招待、App Store審査提出、課金開始は今回の操作に含まない。

## 参照

- [原案](C:/dev/neko-evidence/personal-window-rediscovery-design-20260914.md)
- [検討結果](C:/dev/neko-evidence/personal-window-rediscovery-review-20260914.md)
- [166の写真直接表示](2026-09-14-widget-direct-photo.md)
- [開発・配布の既存手順](development-release-workflow.md)
