# 写真から、その猫のまどへ

2026-09-13。ユーザーの「まだアプリを開発したい」「進めて」を受け、LP掲載合わせを保留し、合意済みの公開まど第4段階を進める。

## 今回の範囲

- 公開写真の下の「この猫のまど」から内容を確認し、既存の「このまどを受け取る」で追加する。戻ると元の写真へ戻れる。
- 最初は「キジ白のまど」1つ。`cat-tabby-nap` → `generated-tabby-nap` の明示的な対応を使う。現在承認済みのAI画像 `generated-20260913-tabby-nap` が対象。同名のキジ白 `generated-tabby-v3` は別の猫IDであり混ぜない。
- 元の「どこかの猫」や「おひるね」の購読、配置済みWidgetの表示元を勝手に変更しない。猫のまども個別に停止できる。
- 登録されていない猫には使えない入口を出さず、猫のまど内には同じまどへのリンクを重ねない。
- 写真主体の表示、拡大、短いひとこと、提供元のAI表記を維持。私的共有のハートは変更しない。

## 完了条件と検証

1. 元の写真 → 猫のまど → 受け取り → そのまどのWidget案内 → 元の写真、が通る。購読先と戻り先をUIテストで確認。
2. 最大文字設定でも入口・写真・停止に進める。猫のまどの停止が元の購読へ波及しない。
3. 猫IDの不一致をプレビュー、取得、保存済み状態、画像読み出しで拒否し、Widgetも同じ保存境界を通る。期限切れでも購読意思は保持する。
4. 配信bundleは既存まど・履歴・休止を引き継ぐ。初回写真の掲載日・期限・hash・提供元を変えずに参照し、新規掲載と偽らない。将来の猫窓への追加も同じ猫IDに限定する。
5. 対象のローカル検証・独立レビュー → 候補CIを1回 → main → 既存の内部TestFlight。Appleアップロード成功を配布作業の証拠にし、毎回のASC画面照合はしない。

実装・検証・配信・実機を区別する。着手時点ではCI、preview配信への反映、TestFlightは未実施。下記に実測結果を追記する。

## 今回含めないこと

投稿受付、猫の自動識別、課金、新しいSNSや公開ハート、一般公開は含まない。この猫の継続供給用の新画像は今回生成しない。現在の1枚の掲載期限は9月20日16:42 JSTで、供給不足の際に別猫を代わりに出さない。

## 運用の引き継ぎ

手動配備も既存 `output/runtime/pending.json` の排他と配備前後のversion/HTTP照合を使う。配備後は固定保守checkoutのツールと `current.json` を引き継ぐ。旧2まどだけのbundleへ定期保守が戻さないよう、猫のまどを含むbundle・制約付き履歴を新しい基点にする。

## 実装・配信・検証の結果

製品SHA `c2c3118fb38a8033117713c546463372b353adcd`。写真の詳細に「この猫のまど」を追加し、受け取り・Widget案内・元の写真への帰路を接続した。公開まど一覧のカードが見た目の外までタップを拾う問題も修正。最大文字では説明部分だけをスクロールし、猫のまどへの入口を画面内に保つ。

- Serviceの対象検証は83件成功。猫ID制約、掲載履歴、取り下げ、既存まどを保持する更新を含む。共有Swiftの保存境界とUI/配信差分は担当を分けてレビューした。
- 最初の候補CI `34750848722` は新規UIテスト2件で停止。窓カードのタップ範囲による別窓の選択は製品不具合、重なった画面の同名要素を拾う問題はテストの指定不足として修正した。最大文字で入口が下端へはみ出す点も実画面で確認して直した。
- 修正版の候補CI [34752139364](https://github.com/soso-so-27/neko-widget/actions/runs/34752139364) は全8ジョブ成功、経過38分23秒。うち共有画面の自動確認36分59秒。これに初回候補や実装時間は含まない。
- 同じCIの撮影画像4枚（通常・最大文字の写真詳細、猫のまどのWidget案内、受け取り後の一覧）を目視確認。新しい2つのUIテストで、正しい窓の選択、元の写真へ戻る、元の購読を保ったまま猫のまどだけ停止する流れが成功。合成fixtureによる確認であり、利用者の実機確認の代用とはしない。
- main CI [34753827285](https://github.com/soso-so-27/neko-widget/actions/runs/34753827285) は同じSHAの成功証拠を再利用して成功。CI構成の変更や、成功済み検証の重複実行は追加していない。

配信は2026-09-13 19:05 JSTに全3まどのHTTPとJPEG hashを照合済み。Worker版 `4efc762a-517e-478e-938d-94fabbce641c`、配信枚数はどこかの猫4枚・おひるね2枚・キジ白のまど1枚。新しいbundleは `C:/dev/neko-cat-window-20260913/output/cat-window-20260913/edition-001/bundle`。固定保守checkoutとcurrent.jsonを更新し、次の保守で旧2まどへ戻らない状態へ引き継いだ。

catalog期限は9月15日19:01 JST、次の期限保守判断は9月14日19:01 JST。既存queueは3枚のまま。ただしキジ白のまどと同じ猫IDの在庫は0枚であり、この猫の新規写真の継続供給は未完了。既存写真の期限は9月20日16:42 JSTのまま。

## 残る確認

利用者の実機での追加・Widgetの表示と対象写真への遷移は未確認。最初の定期保守の自動実行と、予定写真の実配信も別項目として残す。これらを未実装や、他の本線開発の停止条件には戻さない。


## TestFlight

TestFlight **1.0 (162)** は2026-09-13 20:22 JSTにAppleへのアップロード成功。[run34753891536](https://github.com/soso-so-27/neko-widget/actions/runs/34753891536) の実行ログで `UPLOAD SUCCEEDED with no errors` を確認した。製品SHAは上記c2c3118。固定release helperのdry-run後に同じ引数で1回dispatchし、既存testflight環境の承認を行った。

Apple側の処理完了・内部グループ画面の再照合・利用者の実機は未確認。毎回のASC再ログインや画面確認は追加していない。外部テスター招待、App Store一般公開、課金、LP変更は行っていない。

## 20:40追記：継続供給の初回補充

[次の写真3枚を用意](2026-09-13-cat-window-supply.md)し、あくび1枚を配信、残り2枚を9/16・9/20に予定した。現在の猫別は2枚で、上の「追加在庫0枚」は初回実装時点の記録となる。アプリは162のまま。
