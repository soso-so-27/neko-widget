# 変更に関係する画面だけを確認する

2026-09-14。ユーザーの「毎回42件すべてを回す必要はない」という承認に基づくCI変更。起点は `origin/main b33fac4`、専用branch `codex/ci-targeted-tests-20260914`。アプリの製品コードは変更しない。

## 変更する実行範囲

| 変更 | アプリ画面操作 | Widget描画 | 実写真の起動・スキャン |
|---|---|---|---|
| 指定したWidget専用処理 | 関連7件 | 通常＋個人の使用前後 | 実Photos権限1件＋既存の後段検証 |
| Widgetの配置・操作を含む変更 | 関連7件 | 上記＋白背景/長文/大文字＋ひとことなし | 同上 |
| Widgetの文字・余白など、既知の表示行だけ | アプリ画面操作なし | 通常・個人・白背景/長文/大文字・ひとことなし | 同上 |
| 既存の写真/公式画面の限定された文言・配置 | 従来の関連する画面群 | なし | 同上（別OSでの20件の重複操作を除く） |
| 製品・既存ビルド/安全確認が不変の、既知のCI選択設定だけ | 実行経路の関連7件 | 通常＋個人の使用前後 | 実Photos権限1件＋後段検証 |
| 共通起動、権限、共通保存/送信、署名、その他CI、未知の変更 | 従来42件 | 従来の全条件 | 従来21件＋後段検証 |

ビルド、privacy、署名構成、migration、2OSの共有runtime、nativeの日付・回数・競合・鍵・送信先の境界検証は残す。変更のない画面操作を減らすのであり、ロジック検証を画面テストへ押し付けない。

## Widget関連7件の役割

- 個人・非公開・公式・猫別WidgetのURLをcold/warmで開く、欠損写真を別写真へ置き換えない：3件。
- 日次選び直しの履歴/前の一枚、候補が1枚の場合の既存写真操作：2件。
- 写真の対象範囲外から開いたWidget写真から戻る：1件。
- 受信写真の保存・ハートが表示中の写真と送信状態に結び付く：1件。

全42件からmethod単位で選ぶ。猫管理や思い出の月別導線、送信入力欄、公式まどの購読設定一式を、Widget専用の変更では繰り返さない。実際のホーム画面でOSがAppIntentを渡す挙動や、UI/UXの楽しさは、このテスト成功だけで確認済みにしない。

## 対象の判定と証拠

`ios_ci_scope.py` にWidget専用の既存11パスを明記。共通のWidgetCacheBuilder、WidgetRenderPlan、MomentSharingCoordinator/APIClient、共有storeは別画面にも使われるため、この対象へ丸ごと加えない。新しい製品ファイル、削除、rename、mode/type変更、inline fixture/条件分岐の変更は一式へ戻す。手動workflow_dispatchも常に一式。

したがって、Build 169の変更全体を後から「7件だけで十分だった」とは扱わない。169は共通通信処理と検証コードも変更しており、今回のパス判定では一式になる。日常のWidget専用変更を、その共通変更と同じ扱いにしないための最初の切り分け。

CI選択コード自身については、既知の選択器・対応テスト・smoke引数生成・workflow配線だけを `ci-selection-v1` とする。最新origin/mainが候補のancestorで、全branch差分がこの集合とhandoffだけであることを確認する。workflowは既知のscope出力・条件・名前・並列枠以外を不変とし、既存build steps・permissions・署名設定に差分があれば一式へ戻す。smokeも選択区間と3引数→配列の置換以外が同一と確認する。指定した新規テスト2パスだけ100644での追加を認め、削除・rename・型変更は認めない。製品変更との混在や判定不能も一式。

製品変更へ添えた `handoffs/*.md` だけで一式へ戻さない。文書でも実行属性・symlinkは除外し、製品の新規追加を文書扱いしない。

smokeは一式とPhotosだけでジョブ名を分ける。各scopeの必須jobと実際の選択引数・成果物を同じ定義から作る。全件成功は同じlaneの限定確認を満たせるが、限定成功は一式や別laneを代替しない。失敗・skip・欠落・重複・別SHAは従来どおり拒否。

表示だけの変更ではapp-ui jobを開始せず、その空いたMac枠を描画へ使う。どのscopeも同時使用の上限は5台以内。UIの2群分割は今回追加しない。

## 検証と導入記録

選択/証拠の独立テスト、実Gitの差分・文書・属性境界、実在する7method、workflow展開、空の選択を拒否するshell、配布CLIとの整合を安価な開発チェックで確認する。この候補は上記のCI選択専用scopeで実ビルド・2OS runtime・Photos1件とスキャン・UI7件・通常/個人Galleryを実行する。一式を機械的に実行する方針は、製品不変の今回に対して過大と判断して変更した。全scopeの生成・条件・証拠境界は安価なテストで検証し、全件や白背景等の既存選択も保持する。成功済みの同じSHAはmainで再利用し、CI変更だけの追加TestFlightは作らない。

この時点では、限定scopeの実Mac時間は未計測。42→7件、重複20件の除去は選択内容の変更であり、「何分で配布できる」という実測結果ではない。結果を追記する。
