# 猫の入口と3タブのタイトルを統一

利用者が承認した、両タブに猫の入口を残して見せ方を揃えるバッチ。起点は origin/main `49b7c8d`。独立worktree `C:/dev/neko-cat-navigation-20260917`、branch `codex/cat-navigation-consistency-20260917`。

## 変更・完了条件

- アルバムと写真の先頭を共通の猫入口にする。顔写真28pt・名前・進む矢印、同じプロフィール順、44pt以上の操作領域。登録済み猫は横送りですべて選べ、最大文字では縦に並ぶ。右端の省略アイコンは、そのタブの猫一覧を開く。
- 写真の猫入口はその猫の写真一覧、アルバムはその猫のアルバムへ進む。既存の「猫名の写真／アルバム」のタイトルを維持。画面全体の選択状態や共有フィルターは作らない。
- 写真とまどのタイトルをアルバムと同じ inline に揃える。3タブの現在地は小さなタイトルと既存のタブラベルで示す。
- 猫の未登録時は写真の既存登録入口を維持。保存・写真の所属・月/動画の範囲・Widgetは変更しない。

写真とアルバムの同じfixtureの猫で標準描画を比較し、既存の最大文字・猫別へ進んで戻る操作を確認する。既存UIテストへ描画1枚を追加するだけで、テスト件数は増やさない。候補CI・main証拠再利用・内部TestFlightまで進める。CI選択や公開範囲を変更しない。

## 検証・配布

- 差分チェック、開発フロー8群（46.7秒）、既存境界63件（1件は環境条件でskip）は成功。
- 製品SHA `bee0fe1279d8f437102451e2f713fae55734b497`。候補[CI 35190136458](https://github.com/soso-so-27/neko-widget/actions/runs/35190136458)は最終的に必要8項目成功。アプリ画面操作45件は初回ですべて成功。操作本体45分31.5秒、アプリUI job 58分09秒。
- 初回のWidgetギャラリー「ひとことなし」だけ、中→大へのOSページ送りが成立せず失敗。小・中には実写真が表示され、失敗時も中サイズのまま。AX remote serverエラーも併発。製品の写真未描画とは区別し、同じSHAの失敗jobだけ1回再実行、16分35秒で成功。環境だけが根因とは断定しない。成功済みのアプリUI等は再実行していない。
- 同SHAをmainへ反映し、[main CI 35196736323](https://github.com/soso-so-27/neko-widget/actions/runs/35196736323)が候補の成功証拠を再利用して成功。
- 製品PNG5枚を目視確認。同じ猫・代表写真を使う標準Albums/Photosは猫入口の外観・位置が一致し、まどを含む3タブのinlineタイトルを確認。最大文字はPhotosとまどを確認。Photosの猫列全体は中央配置になるが、名前・顔・矢印に欠けはなく、2匹への操作も成功。猫入りAlbumsの最大文字PNGまでは取得していない。
- 画像証拠は `output/app-ui-35190136458/manifest.json` とその5枚。必要な画像だけ部分取得し、大容量ZIPは取得していない。
- 配布CLIのdry-runで前回178、今回179、main/候補CI、SHAと既存内部配布設定を照合して1回dispatch。対象runのtestflight環境だけを承認。[TestFlight 35196852521](https://github.com/soso-so-27/neko-widget/actions/runs/35196852521)成功。**1.0 (179)は9月17日17:02:02 JSTに `UPLOAD SUCCEEDED with no errors`** を確認。Apple処理完了・内部グループ画面・179実機は未確認。一般公開・外部招待・審査提出・課金開始は行っていない。

Build 178は利用者が更新して「画面はよくなった」と確認済み。今回の指摘は猫入口の重複感とタイトルの不統一。過去の全確認を再開せず、この変更を評価する。

## 配布待ち時間の整理（未実装の改善候補）

今回もCI高速化を達成したとは扱わない。MainTab/共通部品/テストの変更が、現在の「既知の文言・装飾行だけ」の判定を超えてfullへ戻る。次に改善するなら、猫入口の描画部品を分離して基準を確認し、その後の許可済み装飾値だけを限定scopeへ送る。MainTab全体を許可せず、タイトル表示モードだけの差分と他の不変を照合する。共有・権限・保存・新規ファイル・fixture・構造変更はfullを維持する。短縮範囲は公式/再発見UIやWidgetギャラリー等で、ビルド・写真系UI・runtime・runner待ちは残り、実測が必要。今回の候補にCI変更は混ぜていない。
