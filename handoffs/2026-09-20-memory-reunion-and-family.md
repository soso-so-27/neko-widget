# メモとの再会・保管・共同記録

2026-09-20。ユーザーの「3まで一気に進めて」に対応。基点 `a082d6b`。新しいタブや独立したメモ作成画面を増やさず、既存の写真中心の流れにつなぐ。

## 今回の変更

1. アルバムの「今日のピックアップ」に、メモ付きの写真を最大1枚含める。全体は既存の最大3枚。写真と短い本文を同じカード形式で表示し、押すと共通の写真・メモ詳細へ進む。端末内のメモを、現在のアルバム対象・権限・除外と照合する。個人の本文をWidgetや相手へ送らない。
2. 保管状況を保管済み／確認が必要で整理し、使い方の詳細は別画面へ。旧コピーだけ保管済みで最新メモが端末に残る状態も再試行できる。[対象・容量・復元・販売前の条件](2026-09-20-preservation-contract.md)。
3. 共同記録は「同じ写真に、それぞれの言葉を」。空の状態から写真追加へ進める。写真／文章の作者を示し、本人の操作は…にまとめる。文章の追加と編集を区別。写真取り下げで相手の言葉が消えない既存動作と、共有終了で開けなくなる条件を説明する。

## 再会の選び方と境界

- UUID順の候補を日付で巡回し、日内は既存のIDスナップショットを維持。候補を保存する場所には本文・画像を追加しない。
- 同じ写真や撮影時刻が近い既存カードとの重複を避ける。既存カードの順番を閲覧中に入れ替えない。当日既に3枚が確定している場合は、その日を維持し翌日から候補になる。
- 写真への権限や対象がなくなればカードに解決しない。元写真が現在のアルバム対象にない保管コピー単独の記録は今回の選定対象外だが、写真タブのメモ一覧では引き続き読める。
- 猫別アルバムやWidgetの日替わり選定は変更しない。通知・連続記録・作成の催促・自動課金は追加しない。

## 検証と配布

- 写真範囲・権限・除外・個人/共有境界を独立レビュー。保存、送信、暗号、CloudKit schema、共有側serviceは不変。保管画面の未完了判定だけ読取を追加。
- 既存UIテスト3件へ今回の経路を追加。メモの再会→詳細→戻る・権限変更、保管案内と端末だけの反映待ち→再試行、共同記録の本人編集・相手本文保持・写真取り下げ・退出後非表示。
- ローカル開発フロー11組は58.5秒で成功。候補 `f9f68d5` のCI `35517941348` は66分26秒、runner合計174.45分。build・権限/実写真スキャン・共有runtime・Widget描画等の7jobは成功。UIは53件中50件成功、3件失敗で配布しない。
- 失敗はテスト準備の不備として切り分けた。再会fixtureの写真9はアルバム対象1〜8の外だったため1へ修正。保管の再試行fixtureは保管側のsource linkがなかったため、正規の明示保管経路で作成。アルバム復帰テストは写真の前回区分を考慮せず猫ショートカットを探していたため「すべて」を明示選択する。失敗動画の最終画面で「メモ」選択を確認した。製品の保存ガード・前回選択保持は変更しない。
- 共同記録の作者表示・本人編集・相手本文保持・取り下げ・退出後非表示はnative成功。共同記録と保管案内の製品描画を確認した。上記3件の修正後native結果とメモカード描画は確認待ち。
- 診断 `35522448005` / `2493715` ではメモ再会とアルバム関連遷移が成功し、メモカードの写真・本文・日付の描画も確認。保管テストは既存の再計算回数判定で失敗した。6更新を150msずつ送るfixtureは、負荷次第で初回worker完了をまたぎ、逐次3回の実行になり得る。最大並列数違反ではない。DEBUGの6更新をMainActorの非中断burstへ修正し、従来の2回上限は維持した。実行中の入力圧縮と最大並列数1は、必須Buildで実行する既存barrier検証が担当する。待ち時間・製品coordinator処理は変更していない。
- 既知の66分一式を盲目的に繰り返さず、失敗3件を同じ候補SHAで診断できる経路と、今回のレビュー済み表示差分に対応するCI選択を独立した基盤候補で検証する。既存の保存・権限・build・安全確認を残す。最初の候補からの累計時間をリセットせず、30分目標を達成したと報告しない。

## 完了を分ける

実装・自動試験・描画・内部アップロードを個別記録する。別の実iPhoneへのiCloud復元、実2端末での共同記録の反映、繰り返し使いたくなる価値と支払い意思は別の未実証事項。これらをmockの成功で完了にしない。一般公開、課金、無期限保管や終了時救済の契約開始は行わない。
