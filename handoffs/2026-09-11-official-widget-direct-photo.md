# 公式Widget：写真を直接開く

## 起点と完了条件

2026-09-11、Build150の画像Bについて利用者から「開きますが、一度どこかの猫が開いてから開くにだんかい？」と報告。受信・実機Widget表示・対象写真への到達は確認済みだが、一覧を経由する遷移は改善対象。

対象写真を最初のsheetで開き、通信完了を待たず有効なキャッシュを表示する。閉じる操作は1回。掲載終了・受信停止・取得できない状態では別の写真を自動で代わりに開かず、明示的な「公式まどを見る」入口を出す。通常の一覧から写真を開く操作は維持する。

## 修正

- 修正 `84d2023`、検証対象 `a2e7cbbb55517e69be54469c1761a6eb45eef248` / `codex/official-window-20260910`。
- 旧動作はAppRootのsheetで一覧を作り、refresh完了後にselectedPhotoを設定して二枚目のsheetを開いていた。
- OfficialWindowViewの初期状態をstoreのsnapshotから取得し、写真ID付きの起動は同じsheet内で直接詳細を描画する。更新処理は並行して続け、有効期限・掲載取り下げ・受信停止の状態も反映する。
- URLを受け取るたび表示用UUIDを更新し、同じWidgetを再タップしたときにも前回の一覧モードを持ち越さない。
- 直接表示では詳細内の閉じるボタンを出さず、AppRoot側の1個で閉じる。
- store、公開catalog形式、Widget描画、配信先、課金には変更なし。研究worktreeは編集していない。

## 検証と配布状態

- 写真権限bootstrapの既存9件とdiff check成功。
- 独立レビュー完了。P1/P2相当の指摘なし。
- UI回帰テストを2本追加。通信を意図的に保留したまま詳細表示を確認し、通信失敗後の写真維持と閉じる1回を確認する。別のテストで対象外IDから違う写真を開かず、明示操作で一覧へ進めることを確認する。既存の受信・再試行・通常詳細・停止テストも維持。
- 初回CI [34545049497](https://github.com/soso-so-27/neko-widget/actions/runs/34545049497)はDEBUG fixtureのassertionFailureに生のerror文字列を追加したため既存の診断ログ検査で停止した。固定文へ直した。検査の緩和なし。Windowsで診断ログ検査12件成功・Swift実行1件環境依存skip。
- 候補CI: [34545153255](https://github.com/soso-so-27/neko-widget/actions/runs/34545153255)。09:16 JSTにアプリ・拡張機能build成功、09:30 JSTにSimulator smoke成功。公式UIテスト4本（新規2・既存2）の実行と添付画像を確認。通常の写真権限テストも同じjobに含まれる。
- rootが通信完了前の直接表示と対象なし画面のPNGを目視確認した。対象写真が最初のsheetの本文として表示され、閉じるボタンは左上に1個。対象なし画面から一覧へのボタンも表示される。下部の「確認用：通信…」はDEBUG fixtureだけの通信操作ボタンで、通常アプリに表示しない。
- 証拠フォルダ: `C:/dev/neko-widget-official-window-evidence-20260910/34545153255/mainline-screen-attachments/`。直接表示 `D4C21EB6-F4DD-4A1E-B3C3-BAD65C080FCC.png`、対象なし `086D0256-097B-44BF-AC08-A13D2EFD5AE5.png`、テスト対応は `manifest.json`。
- 記録時点でCI全体のsharing runtime jobは継続中。今回の対象ビルド・UI検証の成功と、配布前に必要な全job成功を区別する。main反映・次回配布の前には上記runの最終結果を確認する。同じ成功済み対象テストを繰り返さない。
- **TestFlight配布済みはBuild150のまま。この修正は未配布。** 軽微な修正単体で新しい配布は行わず、次の関連バッチへまとめる。
