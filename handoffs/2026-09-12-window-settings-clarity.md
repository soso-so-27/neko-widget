# まど設定の文字密度を整理 — Build155後

起点: 最新main `0baeae4`。worktree `C:/dev/neko-window-settings-20260912` / branch `codex/window-settings-clarity-20260912`。研究worktreeは対象外。

## 今回の範囲と完了条件

- まど設定: 最大Dynamic Typeで装飾アイコンが本文の幅を奪わず、設定名・操作が読める。通常時の重複補足を減らし、通知の許可状態、共有終了、接続未確認の表示と既存操作は保つ。
- 接続管理: 接続済みの確認フレーズは必要時に展開。承認前・iPhone追加時のフレーズ照合や承認条件は変えない。現行のアプリ内送信にも合う説明にする。
- 確認: 既存の共有/通知/ペアリング境界、候補CI、既存native設定画面の通常/最大文字を使う。変更に無関係な試験・新しいfixtureは追加しない。成功した候補をmainと既存内部TestFlightへまとめる。

## 判断の根拠

Build155のnative最大文字画像で、40pt固定枠から拡大した装飾アイコンがはみ出し、管理行の長い通常説明とチェック記号が本文を狭めていた。通知・管理・Widget案内で同じ装飾を揃え、アクセシビリティサイズでは本文の幅を優先する。文字サイズ自体は下げない。

別担当は周辺の追加/管理画面を限定レビュー。追加画面は既存画像で区分・再開操作を確認でき、具体的な崩れは認めなかった。接続済みフレーズの常設はコードで確認した改善候補であり、当該画面の最大文字画像はまだ確認していない。

## 引き続き確認が必要なもの

- R06: 普段の写真の選定・成長比較・月/季節の振り返りの満足。
- R07: 初見の人がまどの種類や追加先を理解できるか。
- R08: 本人の原本とプレビューの画質比較、大量写真での実機負荷。

これらをコード/合成fixtureの成功で完了扱いしない。写真が主役、ひとこととハートは任意。課金・審査提出・外部テスター追加は対象外。

## 実装と差分レビュー

- Widget案内と「名前と接続」は通常補足を削除。通知は短い見出しと既存の許可状態・操作を組にし、読み上げ名は「写真とハートの通知」を維持。
- 設定の装飾は通常18pt/32pt枠へ統一。accessibilityサイズでは装飾列を省き、テキストのサイズや内容を縮めずに幅を確保。安全とプライバシーの開閉は44pt以上を維持。
- 接続済みの確認フレーズはDisclosureGroupで全文展開。承認待ち/承認必須/端末追加の処理は差分外。主担当がこの境界をレビュー。
- 別担当がFamilyWindowView差分を独立レビューし、状態文言・VoiceOver名/ヒント・遷移/通知操作の具体的な回帰は認めず。最大文字の実画面と実際の読み上げ順は別途。
- cheap checks: 共有/Widget境界61件（既存1件skip）、通知18件、pairing構成7件成功。既存2assertだけ新しい設定名へ追従。新規fixture/テストやCI変更は追加していない。
